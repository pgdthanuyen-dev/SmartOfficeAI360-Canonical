# QLVB business workflow

Updated: 2026-07-24

Process Văn bản vào sổ, Đã chuyển xử lý, then Đã xử lý. Chờ xử lý is not used by the default workflow. Select only one validated table row at a time, resolve its row-scoped identifier, and stop at configured bounds.
